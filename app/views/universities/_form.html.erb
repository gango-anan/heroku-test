<%= form_with(model: university) do |form| %>
  <% if university.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(university.errors.count, "error") %> prohibited this university from being saved:</h2>

      <ul>
        <% university.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :start_year %>
    <%= form.number_field :start_year %>
  </div>

  <div class="field">
    <%= form.label :num_faculties %>
    <%= form.number_field :num_faculties %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
